name: Update Production Server

on:
    push:
        branches:
            - main

jobs:
    deploy:
        runs-on: self-hosted

        steps:
            - name: Checkout code
              uses: actions/checkout@v3
            - name: echo stuff
              run: echo "Hi there"